{
  "name": "microgifter-homeserver",
  "private": true,
  "version": "0.1.3",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "prepare:icons": "node scripts/prepare-icons.mjs",
    "preview": "vite preview",
    "tauri": "tauri",
    "tauri:dev": "npm run prepare:icons && tauri dev",
    "tauri:build": "npm run prepare:icons && tauri build",
    "check:frontend": "node --check src/main.js && node --check vite.config.js && node --check scripts/prepare-icons.mjs"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "vite": "^7.0.0"
  }
}
