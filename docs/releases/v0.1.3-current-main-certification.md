# HomeServer v0.1.3 Current Main Certification

**Certification base:** `7b4142615ec5b35df7c4e7282df29c854b7fc198`  
**Release version:** `0.1.3`  
**Scope:** Certification only. No product features are introduced by this pull request.

## Purpose

This pull request creates an exact-head release gate for the current HomeServer `main` baseline after the ownership and update-service architecture was finalized.

The resulting pull-request head must be treated as releasable only after every required workflow completes successfully on that exact commit.

## Required validation

- Dependency lock regeneration produces no `Cargo.lock` or `package-lock.json` drift.
- Frontend syntax and production build pass.
- Security-boundary regression validators pass.
- Native HomeServer service, updater, MCP bridge, and Control Center build successfully.
- Full workspace tests pass.
- Strict workspace lint and formatting pass.
- Service, cloud-connection, backup, and signed-update API smoke tests pass.
- The Windows NSIS installer builds successfully.
- Fresh installation under LocalSystem passes.
- Upgrade installation passes without losing pairing, credentials, grants, backups, or local data.
- Installed release registration and exact version verification pass.
- Signed update installation and automatic rollback pass.
- Windows tray startup, close-to-tray, explicit UI quit, and service independence pass.
- Agent Chat remains responsive.
- Microgifter connection and Sync Code pairing contracts remain compatible.
- Local MCP remains loopback-only and credential protected.
- Coordinated provider/cloud contract validation passes.

## Required release evidence

Record after successful validation:

- Final certified head SHA
- Production Quality workflow run
- Coordinated provider/cloud contract workflow run
- Verified artifact name
- Artifact ZIP SHA-256
- Installer SHA-256
- Fresh-install result
- Upgrade result
- Signed-update result
- Rollback result

## Merge rule

Do not merge this pull request based only on mergeability. Merge only when the exact current head has completed the required validation successfully and the release evidence is recorded in the pull request.

## Next product phase

After this certification merges, the next scoped build is pairing-first HomeServer onboarding inside Agent Workspace. Agent Workspace will become the default Control Center screen and will present connection setup, server lifecycle history, user activity history, and a concise summary of what changed since the user was last active.
