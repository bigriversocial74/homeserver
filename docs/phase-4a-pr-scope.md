# Phase 4A draft PR scope

Build a local-only Knowledge Vault foundation with managed text-document imports, bounded indexing, SQLite metadata, duplicate prevention, local search, change detection, explicit deletion, audit events, a Tauri file bridge, and live Control Center state.

The draft must remain unmerged until generated product files replace the temporary bootstrap mechanism and the full Windows production-quality and cloud-contract suites pass on one immutable head.
